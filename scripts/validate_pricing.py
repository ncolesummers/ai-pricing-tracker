#!/usr/bin/env python3
"""
Pricing Data Validator - Entry point script
Imports and runs the validator from the installed package
"""

# Import only what we need from the package
from ai_pricing_tracker.scripts.validate_pricing import main

# Run the main function from the package when this script is executed
if __name__ == "__main__":
    main()
